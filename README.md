# Screenshots

This branch holds **only screenshots** used by the project README and store listings.

It is an orphan branch (no code history) — kept separate so binary images don't
bloat the source branches. Drop new screenshots here, then reference them from the
main README with:

```
https://github.com/AlirezaParsi/COPG/blob/screenshots/<file-name>?raw=true
```
